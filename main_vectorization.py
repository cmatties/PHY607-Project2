from experiment_vectorized import harmonic_equilibrium_checks, mb_speed_histogram, pressure_temperature

if __name__ == "__main__":
    harmonic_equilibrium_checks(seed = 200)
    #mb_speed_histogram()
    #pressure_temperature()
    plt.show()
